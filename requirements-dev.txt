# Development and testing dependencies
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
httpx==0.28.1        # also used as async test client
respx==0.21.1        # mock httpx requests in tests
vcrpy==6.0.2         # record/replay external HTTP calls
