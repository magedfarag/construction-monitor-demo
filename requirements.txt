# Core API framework
# NOTE: Pin minor version for reproducibility; update after integration testing.
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# HTTP client (async + sync, used for provider STAC calls)
httpx>=0.27.0,<1.0.0

# Resilience: retry with exponential backoff + jitter
tenacity>=8.2.0,<10.0.0

# Async task queue (requires Redis; optional — see README)
celery[redis]>=5.3.0,<6.0.0
redis>=5.0.0,<8.0.0

# Caching fallback when Redis is unavailable
cachetools>=5.3.0,<6.0.0

# Geospatial / raster processing
# WINDOWS NOTE: rasterio requires GDAL. Install via: conda install -c conda-forge rasterio
# or download wheels from https://github.com/cgohlke/geospatial-wheels/releases
# rasterio 1.4+ is required for numpy 2.x support and native HTTPS/COG streaming.
rasterio>=1.4.0
shapely>=2.0.0
pyproj>=3.6.0
numpy>=1.24.0
scipy>=1.12.0
scikit-image>=0.21.0

# Image encoding for thumbnails
Pillow>=10.0.0

# Database persistence (optional — for PostgreSQL job history)
sqlalchemy>=2.0.0,<3.0.0
psycopg2-binary>=2.9.0,<3.0.0

# S3-compatible object storage (MinIO local / AWS S3 production)
boto3>=1.34.0,<2.0.0
